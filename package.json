{
  "name": "bubble-battle",
  "version": "1.0.0",
  "type": "module",
  "description": "Bubble Battle: Campus Chaos",
  "main": "src/main.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:browser": "node scripts/verify-browser.js",
    "qa": "npm run test && npm run build && npm run test:browser"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "vite": "^6.4.3",
    "vitest": "^4.1.10"
  }
}
